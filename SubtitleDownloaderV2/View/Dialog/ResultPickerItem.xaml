<UserControl x:Class="SubtitleDownloader.View.Dialog.ResultPickerItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SubtitleDownloader.View.Dialog"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="{Binding Name}" Foreground="Black" FontSize="18" Padding="0" Margin="0"/>
        <Label Grid.Row="1" Content="{Binding Address}" Foreground="Black" FontSize="10" Padding="0 2 0 0" Margin="0"/>
    </Grid>
    
</UserControl>
